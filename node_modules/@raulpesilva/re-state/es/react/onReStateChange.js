import { store } from './store';
export var onReStateChange = function onReStateChange(callback, dependencies) {
  dependencies.forEach(function (dependency) {
    return store.subscribe(dependency, callback);
  });
};